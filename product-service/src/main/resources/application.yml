spring:
  data:
   mongodb.uri: mongodb://localhost:27017/product_service
  application:
    name: product-service

server:
  port: 0


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true